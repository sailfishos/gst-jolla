SUBDIRS = gst-libs gst

EXTRA_DIST = autogen.sh \
	     libgstnativebuffer.pc.in \
	     libgstnativebuffer.pc \
	     libgstgralloc.pc.in \
	     libgstgralloc.pc

pkgconfigdir = $(libdir)/pkgconfig
pkgconfig_DATA = libgstnativebuffer.pc \
		 libgstgralloc.pc
